# My Letter Substitutions

```
'TRE' is most likely 'THE' and since the generated mapping is 'p -> R'
inside my character mapping 'p -> H'
```

```
'AOD' is most likely 'AND' and since the generated mapping is 'k -> O'
inside my character mapping 'k -> N'
```

```
'ESSENUE' is most likely 'ESSENCE' and since the generated mapping is 'v -> U'
inside my character mapping 'v -> C'
```

```
'MIVEMENT' is most likely 'MOVEMENT' and since the generated mapping is 'j -> I'
inside my character mapping 'j -> O'
```

```
'SOFND' is most likely 'SOUND' and since the generated mapping is 'n -> F'
inside my character mapping 'n -> U'
```

```
'GECAUSE' is most likely 'BECAUSE' and since the generated mapping is 'l -> G'
inside my character mapping 'l -> B'
```

```
'BASNC' is most likely 'BASIC' and since the generated mapping is 'w -> N'
inside my character mapping 'w -> I'
```

```
'COMYLETE' is most likely 'COMPLETE' and since the generated mapping is 's -> Y'
inside my character mapping 's -> P'
```

```
'EKPLANATION' is most likely 'EXPLANATION' and since the generated mapping is 'a -> K'
inside my character mapping 'a -> X'
```

```
'PHACTICE' is most likely 'PRACTICE' and since the generated mapping is 'u -> H'
inside my character mapping 'u -> R'
```

```
'AUTHORITW' is most likely 'AUTHORITY' and since the generated mapping is 't -> W'
inside my character mapping 't -> Y'
```

```
'ENLIBHTENED' is most likely 'ENLIGHTENED' and since the generated mapping is 'o -> B'
inside my character mapping 'o -> G'
```

```
'MYSELC' is most likely 'MYSELF' and since the generated mapping is 'x -> C'
inside my character mapping 'x -> F'
```

```
'FOLLOXING' is most likely 'FOLLOWING' and since the generated mapping is 'c -> X'
inside my character mapping 'c -> W'
```

```
'RECOGNIJING' is most likely 'RECOGNIZING' and since the generated mapping is 'g -> J'
inside my character mapping 'g -> Z'
```

```
'TASP' is most likely 'TASK' and since the generated mapping is 'q -> P'
inside my character mapping 'q -> K'
```
